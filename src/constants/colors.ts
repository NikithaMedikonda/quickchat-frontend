export const useThemeColors = () => {
  return {
    primaryBlue: '#1877F2',
    gray: '#898989',
    background: '#000000',
    white: '#FFFFFF',
    text: '#FFFFFF',
  };
};

export type colors = {
  primaryBlue: string;
  gray: string;
  background: string;
  white: string;
  text: string;
};
